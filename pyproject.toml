[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "unet"                # 安装后包名：import unet
version = "0.1.0"
description = "UNet model package"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["unet"]
